import { NestMiddleware } from '@nestjs/common';
import { ReferrerPolicyOptions } from 'helmet/dist/types/middlewares/referrer-policy';
export declare class HelmetReferrerPolicyMiddleware implements NestMiddleware {
    static configure(opts: ReferrerPolicyOptions): void;
    private static options;
    use(req: any, res: any, next: any): void;
}
