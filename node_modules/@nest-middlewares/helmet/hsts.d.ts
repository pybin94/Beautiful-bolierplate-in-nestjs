import { NestMiddleware } from "@nestjs/common";
import { StrictTransportSecurityOptions } from "helmet/dist/types/middlewares/strict-transport-security";
export declare class HelmetHstsMiddleware implements NestMiddleware {
    static configure(opts: StrictTransportSecurityOptions): void;
    private static options;
    use(req: any, res: any, next: any): void;
}
